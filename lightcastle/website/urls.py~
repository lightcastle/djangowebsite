from django.conf.urls import patterns, url

from website import views

urlpatterns = patterns('',
    url(r'^$', views.login, name='login'), # regex is a wildcard? calls the index method in views.py, for url that matches 'index'?? (name makes it universally referrable
    url(r'^$', views.index, name='login'),

)
